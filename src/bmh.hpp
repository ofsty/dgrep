#pragma once
#include <vector>
#include <string>

void xx44(const std::string& pat, std::vector<int>& table);
size_t zz99(const char* data, size_t len, const std::string& pat, const std::vector<int>& table, std::vector<size_t>& res);
void pp77(const std::vector<char>& buf, size_t st, size_t ed, const std::string& pat, const std::vector<int>& tbl, size_t& cnt, size_t line);
